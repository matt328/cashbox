describe('BudgetItemsService', () => {
  // Create wrappers around angularfire stuff, don't mock what you don't own
  // let angularFirestore: Mock<AngularFirestore>;
  // let service: BudgetItemsService;
  // beforeEach(() => {
  //   angularFirestore = new Mock<AngularFirestore>({
  //     collection: <T>(name: string) => ({
  //       stateChanges: () => EMPTY,
  //     }),
  //   });
  //   service = new BudgetItemsService(angularFirestore.Object);
  // });
  // it('should be created', () => {
  //   expect(service).toBeTruthy();
  // });
});
